# 3D assets

Runtime models use GLB/glTF 2.0. The current shell intentionally ships with primitive fallbacks so missing art never blocks play.

When a model is added, validate its scale, pivot, stable node names, material reuse, collision assumptions, and compressed delivery size before replacing a fallback.
